## Setup — Backend
1. cd backend
2. python -m venv venv
3. activate venv (Windows: venv\Scripts\activate)
4. pip install fastapi uvicorn
5. Create a .env file and add your API keys (ask team lead for format)
6. uvicorn main:app --reload

## Setup — Frontend
1. cd frontend
2. npm install
3. npm run dev